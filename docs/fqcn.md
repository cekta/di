# FQCN
**default**: \App\Container

Указывается полное имя класса вместе с namespace который будет сгенерирован.  
Из этого fqcn будет взят namespace (если он есть) и имя класса (краткое).

В случае если нет namespace класс генерируется без namespace.